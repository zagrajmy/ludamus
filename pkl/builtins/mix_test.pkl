import "../Builtins.pkl"
import "../Config.pkl"

@Builtins.meta {
  category = "Elixir"
  description = "Test Elixir with Mix"
}
mix_test = new Config.Step {
  glob = List("**/*.ex", "**/*.exs")
  exclude = List("deps/**/*")
  check = "mix test --warnings-as-errors {{files}}"
}
