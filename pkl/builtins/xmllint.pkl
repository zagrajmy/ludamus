import "../Builtins.pkl"
import "../Config.pkl"

@Builtins.meta {
  category = "Data Formats"
  description = "XML validator"
}
xmllint = new Config.Step {
  glob = "**/*.xml"
  check = "xmllint --noout {{ files }}"
}
