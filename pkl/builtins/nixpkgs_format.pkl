import "../Builtins.pkl"
import "../Config.pkl"

@Builtins.meta {
  category = "Nix"
  description = "Nixpkgs formatter"
}
nixpkgs_format = new Config.Step {
  glob = "**/*.nix"
  check = "nixpkgs-fmt --check {{ files }}"
  fix = "nixpkgs-fmt {{ files }}"
}
