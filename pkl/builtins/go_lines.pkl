import "../Builtins.pkl"
import "../Config.pkl"

@Builtins.meta {
  category = "Go"
  description = "Long line fixer"
}
go_lines = new Config.Step {
  glob = "**/*.go"
  check = "golines --dry-run {{ files }}"
  fix = "golines -w {{ files }}"
}
